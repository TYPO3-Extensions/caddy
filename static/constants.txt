###
# These are the default TS-constants for caddy
##

plugin.caddy {
  # cat=plugin.caddy//0010; type=file[html,htm,tmpl,txt]; label= Template File: HTML-template file FE output (see EXT:caddy/files/templates/cart.html or EXT:caddy/files/templates/cart_table.html)
  main.template = EXT:caddy/files/templates/cart.html

  # cat=plugin.caddy//0020; type=text; label= Decimal (e.g. 2). This setting will be used for formatting prices.
  main.decimal = 2

  # cat=plugin.caddy//0030; type=text; label= Decimal symbol (e.g. , or .). This setting will be used for formatting prices.
  main.dec_point = ,

  # cat=plugin.caddy//0040; type=text; label= Thousands separator (e.g. , or .). This setting will be used for formatting prices.
  main.thousands_sep = .

  # cat=plugin.caddy//0050; type=text; label= Currency symbol (e.g. &euro or $).
  main.currencySymbol = &euro;

  # cat=plugin.caddy//0055; type=boolean; label= Show currency symbol before price.
  main.currencySymbolBeforePrice = 0

  # cat=plugin.caddy//0060; type=text; label= Quantity symbol (e.g. St.).
  main.quantitySymbol = St.

  # cat=plugin.caddy//0070; type=text; label= Service Attribute 1 symbol (e.g. Weight: g, kg, lbs).
  main.service_attribute_1_symbol =

  # cat=plugin.caddy//0071; type=text; label= Service Attribute 2 symbol (e.g. Volume: l, gal).
  main.service_attribute_2_symbol =

  # cat=plugin.caddy//0072; type=text; label= Service Attribute 3 symbol (e.g. Length: m, cm, inch).
  main.service_attribute_3_symbol =

  # cat=plugin.caddy//0090; type=text; label= Enter the uid of the page where caddy plugin resides (e.g. 10).
  main.pid =

  # cat=plugin.caddy//0100; type=text; label= Param PUID: Enter the correct parameter name for a unique product number (integer) of your order form. caddy uses this number for internal purposes. It can't be a string. The PUID will never be displayed (e.g. tx_browser_pi1|showUid).
  gpvar.puid =

  # cat=plugin.caddy//0105; type=text; label= Param CID: Enter the correct parameter name for the uid of the current tt_content element. This value is only needed if you want to add tt_content elements to your cart. The provided parameter must be equal to the name in your HTML template. Default: cid.
  gpvar.cid = cid

  # cat=plugin.caddy//0110; type=text; label= Param Quantity: Enter the correct parameter name for the quantity/amount field of your order form (e.g. tx_trbooks_qty). The provided parameter must be equal to the name in your HTML template. Default: qty.
  gpvar.qty = qty

  # cat=plugin.caddy//0115; type=text; label= Param Title: Enter the correct parameter name for the title field in your order form (e.g. title).
  gpvar.title = title

  # cat=plugin.caddy//0120; type=text; label= Param Price: Enter the correct parameter name for the price field in your order form (e.g. price).
  gpvar.price = price

  # cat=plugin.caddy//0121; type=text; label= Param Service Attribute 1: Enter the correct parameter name for the service attribute field 1 of your order form (e.g. weight).
  gpvar.service_attribute_1 =

  # cat=plugin.caddy//0122; type=text; label= Param Service Attribute 2: Enter the correct parameter name for the service attribute field 2 of your order form (e.g. volume).
  gpvar.service_attribute_2 =

  # cat=plugin.caddy//0123; type=text; label= Param Service Attribute 3: Enter the correct parameter name for the service attribute field 3 of your order form (e.g. length).
  gpvar.service_attribute_3 =

  # cat=plugin.caddy//0140; type=text; label= Param SKU: Enter the correct parameter name for your SKU (stock keeping unit).
  gpvar.sku =

  # cat=plugin.caddy//0150; type=text; label= Tax rate: Enter the tax rate for reduced items (e.g. 7). Value will be displayed.
  tax.reduced = 7

  # cat=plugin.caddy//0155; type=text; label= Tax rate: Enter the tax rate for reduced items (e.g. 0.07). Used for calculation.
  tax.reducedCalc = 0.07

  # cat=plugin.caddy//0160; type=text; label= Tax rate: Enter the tax rate for normal items (e.g. 19). Value will be displayed.
  tax.normal = 19

  # cat=plugin.caddy//0165; type=text; label= Tax rate: Enter the tax rate for normal items (e.g. 0.19). Used for calculation.
  tax.normalCalc = 0.19

  # cat=plugin.caddy//0200; type=text; label= Table name: Enter the correct table name where the products are stored (e.g. tx_database).
  db.table =

  # cat=plugin.caddy//0210; type=text; label= Title column: Enter the correct column name of the table where the product titles are stored (e.g. title).
  db.title = title

  # cat=plugin.caddy//0220; type=text; label= Price column: Enter the correct column name of the table where the prices are stored (e.g. price).
  db.price = price

  # cat=plugin.caddy//0230; type=text; label= Tax column: Enter the correct column name of the table where the tax categories are stored (e.g. tax).
  db.tax = tax

  # cat=plugin.caddy//0240; type=text; label= SKU column: Enter the correct column name of the table where the SKUs are stored (e.g. sku).
  db.sku =

  # cat=plugin.caddy//0241; type=text; label= MIN column: Enter the correct column name of the table where the MIN order quantities are stored (e.g. min).
  db.min =

  # cat=plugin.caddy//0242; type=text; label= MAX column: Enter the correct column name of the table where the MAX order quantities are stored (e.g. max).
  db.max =

  # cat=plugin.caddy//0250; type=text; label= Service Attribute 1 column: Enter the correct column name of the table where the service attribute 1 are stored (e.g. for weight).
  db.service_attribute_1 =

  # cat=plugin.caddy//0251; type=text; label= Service Attribute 2 column: Enter the correct column name of the table where the service attribute 2 are stored (e.g. for volume).
  db.service_attribute_2 =

  # cat=plugin.caddy//0252; type=text; label= Service Attribute 3 column: Enter the correct column name of the table where the service attribute 3 are stored (e.g. for length).
  db.service_attribute_3 =

  # cat=plugin.caddy//0300; type=int+; label= Powermail content uid: Enter the uid of the powermail content element to hide this element if the cart is empty AND to clear cart if this form was submitted.
  powermailContent.uid =

  # cat=plugin.caddy//0600; type=text; label= Prefix for Order Number.
  ordernumber.prefix =

  # cat=plugin.caddy//0610; type=text; label= Suffix for Order Number.
  ordernumber.suffix =

  # cat=plugin.caddy//0620; type=int+; label= Offset for Order Number: The offset is added to the order number (e.g. for offset 1000000 the first order number will be 1000001).
  ordernumber.offset =

  # cat=plugin.caddy//9999; type=boolean; label= Debug Mode: Enable or disable some debug output.
  debug = 0
}

#clear powermail session data
plugin.powermail.clear.session = 1